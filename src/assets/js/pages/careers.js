/**
 * Careers Page JavaScript
 * Minimal init function for page loader compatibility
 * Page uses CSS animations for all visual effects
 */

/**
 * Initialize Careers page
 * Currently no custom functionality needed - all animations are CSS-based
 */
export function initCareers() {
    console.log('Careers page initialized');
}
