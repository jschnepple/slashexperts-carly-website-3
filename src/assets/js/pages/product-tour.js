/**
 * Product Tour Page JavaScript
 * Minimal functionality - animations are handled via CSS
 */

export function initProductTour() {
    // Page animations are CSS-only (fadeInUp keyframes with delays)
    // Navattic embed is handled via iframe

    // Log initialization for debugging
    console.log('Product Tour page initialized');
}
