# Change Log — Trust Layer / Carly Overhaul

Every content change in this initiative, before → after, for easy comparison and review. Newest groups at the bottom. Nothing pushed to GitHub until Jeff approves the full pass.

---

## Group A — Global meta + footer  ✅ (2026-06-15)

**`src/index.njk` — front-matter title**
- Before: `SlashExperts | Peer Validation Platform for B2B Sales`
- After: `SlashExperts | The Trust Layer for B2B Sales`

**`src/index.njk` — front-matter description**
- Before: `Turn your invisible competition into your competitive advantage with peer-to-peer validation.`
- After: `SlashExperts is the Trust Layer for B2B Sales. Carly, our AI agent, delivers the right customer proof at the exact moment buyers need confidence to move.`

**`src/index.njk` — JSON-LD description (line ~20)**
- Before: `AI-powered peer validation and expert booking platform that connects B2B prospects with existing customers for authentic peer conversations during the sales process.`
- After: `The Trust Layer for B2B Sales. SlashExperts pairs Carly, an AI agent, with a booking system, peer matching, and AI outbound to deliver the right customer proof to prospects at the moment they need confidence to move.`

**`src/_includes/components/footer.njk` — brand tagline**
- Before: `Turn your invisible competition into your competitive advantage. Connect prospects with your happiest customers at the moments that matter most.`
- After: `The Trust Layer for B2B Sales. Carly delivers the right customer proof to your prospects at the moments that matter most.`

---

## Group B — Homepage hero + definitional lead  ✅ (2026-06-15)

**`src/index.njk` — hero badge**
- Before: `Peer Validation Platform for B2B Sales` (linked /how-it-works/)
- After: `The Trust Layer for B2B Sales` (linked /carly-ai-agent/)

**`src/index.njk` — hero H1**
- Before: `Your prospects have 347 conversations about you`
- After: `Meet Carly. Buyer trust, delivered at the decisive moment.`

**`src/index.njk` — hero headline line**
- Before: `You're just not in them. Until now.`
- After: `The AI agent at the core of your trust layer.`

**`src/index.njk` — hero subtitle**
- Before: `While you wait for references to call back, competitors are closing. SlashExperts gives prospects instant access to your best customers—at the exact moment they're deciding.`
- After: `AI made information infinite—so trust is the only thing that closes deals. Carly watches your active deals and delivers the right customer proof, expert, or reference at the exact moment buyers need confidence to move.`

**`src/index.njk` — hero secondary CTA**
- Before: `See How It Works →` (→ /how-it-works/)
- After: `Meet Carly →` (→ /carly-ai-agent/)

**`src/index.njk` — "What is SlashExperts" definitional lead (line ~376)**
- Before: `SlashExperts is a peer validation and expert booking platform built for B2B SaaS revenue teams…`
- After: `SlashExperts is the Trust Layer for B2B Sales, built for B2B SaaS revenue teams. Carly, our AI agent, watches your active deals and delivers the right customer proof at the exact moment buyers need confidence to move…` (kept full capability list; "booking system," ExpertScore AI, 50+ data points)

**Kept intentionally (per "demote, don't scrub"):** "The State of Peer Validation in B2B" research-stats H2 (line ~148/182) and the "invisible competition" closing-CTA hook (line ~1043) — both legitimate supporting usage consistent with the deck.

---
## Group C — Homepage new sections  ✅ (2026-06-15)

**`src/index.njk` — NEW "Why Now" trust-narrative section** (inserted after hero/logo-bar, before research-stats). Badge "Why Now"; H2 "AI made information infinite. Trust became scarce."; 3 cards: Information is infinite → Trust is scarce → Carly delivers it at the decisive moment. Self-contained scoped `<style>` (`.tl-*`) with mobile breakpoint.

**`src/index.njk` — NEW "Four products. One platform." section** (inserted after "What is SlashExperts," before Use Cases). Badge "The Platform"; H2 "Four products. One platform."; 4 cards: 01 Carly AI Agent (NEW) · 02 Booking System · 03 Peer Matching · 04 AI Outbound; "Meet Carly →" CTA. Mirrors deck slide 4.

## Group D — Carly page elevated  ✅ (2026-06-15) — `src/pages/carly-ai-agent.njk`

- title: `Carly — AI Social Proof on Autopilot | SlashExperts` → `Carly — The AI Agent at the Core of Your Trust Layer | SlashExperts`
- meta description → trust-layer aligned ("the AI agent at the core of SlashExperts, the Trust Layer for B2B Sales…").
- hero badge: `Introducing Carly — AI Social Proof, on Autopilot` → `Carly — The AI Agent at the Core of Your Trust Layer`
- hero subhead → "…delivers the right customer proof, expert, or reference to prospects at the exact moment they need confidence to move — the AI agent at the core of your trust layer."
- H1 ("The AI Agent That Sends the Right Proof at the Right Moment") kept — already on-message.
- Cross-linking: homepage hero badge, hero CTA, and Four-Products CTA all link to `/carly-ai-agent/`.

## Group E — Secondary metas, terminology, advocacy  ✅ (2026-06-15)

**Terminology — `src/pages/experts.njk`**
- title: `Expert Directory | SlashExperts` → `Expert Booking System | SlashExperts`
- meta description → "Connect with verified customer experts through the SlashExperts booking system…"
- (Kept widget container id `experts-directory-container` + bundle URLs — technical identifiers, not user-facing.)

**Secondary-page meta descriptions (demote "peer validation" → Trust Layer):** `about.njk`, `book-a-demo.njk`, `careers.njk`, `roi-calculator.njk`, `solutions-customer-success.njk`, `solutions-marketing.njk`, `solutions-sales.njk` — each reframed to lead with "the Trust Layer for B2B Sales" / "real customer proof," peer language kept only as supporting.

**Advocacy removal (brand rule):**
- `solutions-customer-success.njk` meta: "turn customers into advocates" → "turn customers into champions"
- `book-a-demo.njk` (line ~667): "your first expert advocates" → "your first expert champions"

## Group F — Designer animations wired into homepage  ✅ (2026-06-17, local only)

**New files:** `src/assets/animations/carly-hero.html` (5.7MB → **1.25MB**, −78%) and `src/assets/animations/why-now.html` (234KB → **39KB**, −83%). Designer's live HTML animations, image-optimized (embedded images → WebP; 2.27MB dashboard bg → ~22KB) with a responsive scale-to-fit wrapper injected. Animations/fidelity preserved.

**`.eleventy.js`** — added `addPassthroughCopy({"src/assets/animations": "assets/animations"})`.

**`src/index.njk`**
- Hero `.hero-visual` → replaced the old base64 dashboard + 4 profile-card images with a lazy-loaded, responsive `<iframe src="/assets/animations/carly-hero.html">` (aspect 1320/840).
- Why Now section → replaced the 3 `.tl-card` placeholder cards with a lazy-loaded, responsive `<iframe src="/assets/animations/why-now.html">` (aspect 1240/526). Section heading/subtitle kept.
- "Four products. One platform." section untouched.

## Group F — Designer animations integrated  ✅ (2026-06-17, local only)

- Added `src/assets/animations/carly-hero.html` (designer hero, optimized 5.7MB→1.25MB) and `why-now.html` (234KB→39KB); embedded images recompressed to WebP, responsive scale-to-fit shim injected. Live dashboard + card animations preserved.
- `.eleventy.js` — passthrough `src/assets/animations` → `/assets/animations`.
- `src/index.njk` — hero `.hero-visual` placeholder (old base64 dashboard + 4 profile cards) replaced with a lazy responsive `<iframe>` to `/assets/animations/carly-hero.html`; Why Now `.tl-grid` cards replaced with lazy `<iframe>` to `/assets/animations/why-now.html`. New `#anim-embed-css` (aspect-ratio wrappers). Four-products section untouched.

## Group G — Mobile (<768px) treatments  ✅ (2026-06-17, local only)

- **Why Now:** below 768px the animation stays visible, but its three real cards (The Shift / The Consequence / The Answer — animated visuals intact) reflow into a **center-focused carousel** — side cards scaled to 0.82, centered card to full size, scroll-snap + auto-advance + closest-to-center detection (respects reduced-motion). Implemented inside `src/assets/animations/why-now.html`; `index.njk` keeps the iframe visible on mobile at a fixed 460px height. (Superseded the earlier separate text-card carousel, which has been removed.)
- **Hero:** below 768px, inside `carly-hero.html` — `.dash` container chrome (border/padding/glow) hidden so the dashboard fills width; the four `.tcard`s (tl/bl/tr/br) pulled toward center (offsets 30/34→165); grain/dots dimmed; and the scale shim switched to a narrower reference width (1080) with centering so the cluster reads larger. Tablet-and-up pixel-identical.

## Group H — Hero width + responsive polish  ✅ (2026-06-18, local only)

- **Hero graphic width:** the hero animation now breaks out of the centered 1400px text grid into a full-bleed band (`min(94vw, 1600px)`), and the scaler cap was lifted so the scene scales up to fill it (renders larger/wider on desktop). In `index.njk` (`.hero-anim-embed`) + `carly-hero.html` (scaler).
- **Nav at 1024 (993–1200px):** new `nav.css` media query tightens pill gaps/link padding, forces nav items + Sign In + Book a Demo onto one line (`nowrap`), wraps "Become an Expert" to a tidy 2 lines (`max-width:78px`); Book-a-Demo label centered globally (`justify-content/text-align:center` in `buttons.css`).
- **Hero h1 sizing:** base clamp gentled to `clamp(48px,5vw,84px)`; **60px** in the 993–1200 laptop range; **40px** at ≤768 (was 32px); **30px** at ≤480. Tablet-only `<br>` after "delivered" now scoped to 690–768px and hidden on mobile.
- **768 vertical rhythm:** CTA bottom margin 32→8px, social-proof padding trimmed, hero-visual top margin 40→16px.
- **Social proof at 768:** restored to a single compact row (621–768px); stacks to a column only at ≤620px.

<!-- Newer groups appended below as work proceeds -->

---

## 2026-06-18 — Carly page overhaul (LOCAL, unpushed)

`src/pages/carly-ai-agent.njk` + new `src/assets/animations/carly-showcase.html`.

- **New showcase animation** (designer-provided 6-tab feature element): compressed 9.9MB → 1.4MB HTML + 0.6MB images by externalizing 29 deduplicated WebP to `assets/animations/carly-showcase/img/`. Headline changed from "One agent for every part of your pipeline" → **"The right proof, the moment your deal needs it."** Embedded after "Meet Carly"; the old hand-built base64 tab mockup ("section 5") was removed (page 5.46MB → ~430KB).
- **Auto-scroll-on-load bug fixed** inside the animation (inner focus()/scrollIntoView() was yanking the page to the iframe): focus()→preventScroll, autofocus stripped per tab, scrollIntoView→internal-container only.
- **Same-day copy:** "Up and running in under a week" → "the same day"; FAQ "live within one week" → "live the same day — often within a couple of hours"; final badge "Live in under a week" → "Live the same day."
- **Content refresh** (Trust-Layer alignment): Meet-Carly subhead → "The AI agent at the core of your trust layer."; Meet-Carly body → proof-triad (case study / expert / reference, at the decisive moment); "Built different" body → "the AI agent at the core of the Trust Layer."
- **New "One Platform" four-products band** ("Carly doesn't work alone.") after "Three unfair advantages." — 4-card responsive grid (Carly = core, + Booking System, Peer Matching, AI Outbound). Images pending (`assets/images/products/*.webp`).
- **Section reorder:** Problem section moved below the animation. Order: Hero → Meet Carly → See It In Action → Problem → Features → How it works → Built different → Three unfair advantages → Four products → FAQ → CTA.
- **KNOWN OPEN:** showcase animation responsive behavior breaks at ≤1024px (content cut off) — next session (`NEXT_SESSION_CARLY_SHOWCASE_RESPONSIVE.md`).
